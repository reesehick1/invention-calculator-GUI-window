[project]
name = "invention-calulator-gui"
version = "0.1.0"
description = ""
authors = [
    {name = "reesehick1",email = "reeseh1ck@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12,<3.15"
dependencies = [
]

[tool.poetry]
packages = [{include = "invention_calulator_gui", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
